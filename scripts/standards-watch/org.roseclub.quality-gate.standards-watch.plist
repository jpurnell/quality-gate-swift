<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>org.roseclub.quality-gate.standards-watch</string>

    <!-- Absolute path — adjust to where run-standards-watch.sh is installed on the host. -->
    <key>ProgramArguments</key>
    <array>
        <string>/usr/local/custom/share/quality-gate/run-standards-watch.sh</string>
    </array>

    <!-- Weekly: Monday 09:00 local time. Regulations change on the order of years, so
         weekly is ample; the point is that drift can never sit unnoticed for long. -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Weekday</key><integer>1</integer>
        <key>Hour</key><integer>9</integer>
        <key>Minute</key><integer>0</integer>
    </dict>

    <key>RunAtLoad</key>
    <false/>

    <key>StandardOutPath</key>
    <string>/tmp/quality-gate-standards-watch.out</string>
    <key>StandardErrorPath</key>
    <string>/tmp/quality-gate-standards-watch.err</string>
</dict>
</plist>
